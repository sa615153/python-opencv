N-N
random initialization
in the range [-Epsilon_init;,Epsilon_init] 

A good choice of Epsilon_init is Epsilon_init = 6^(1/2)  / (Lin + Lout)^(1/2)
 where Lin = sl and Lout = sl+1 are
the number of units in the layers adjacent to THETA(l).